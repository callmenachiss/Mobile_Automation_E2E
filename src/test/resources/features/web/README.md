# Web feature files (planned)

This folder is reserved for future web automation `.feature` files
(Selenium WebDriver based, browser tests for the same Ecommerce app).

It does not affect the mobile test run today - Cucumber only picks up
files ending in `.feature`, and there are none in this folder yet.
