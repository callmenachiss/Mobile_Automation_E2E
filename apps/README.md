# /apps

Put your Ecommerce app's `.apk` file in this folder.

Then open `src/test/resources/config.properties` and set:

```
app.path=apps/<your-file-name>.apk
```

See the main [README.md](../README.md) for how to find `appPackage` and
`appActivity` for the same app.
